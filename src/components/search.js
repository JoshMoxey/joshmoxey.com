import React, { Component } from "react"

class SearchPage extends Component {
  render() {
    return (
      <div></div>
    )
  }
}

//capture the input
//set redux state
//cry a bit because of confusion
//call an axios request to the server
//drop an existing index if there is one
//create an index for text search
//text search the db
//return results
//render all results out in react